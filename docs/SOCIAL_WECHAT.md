# 微信公众号 · ASCII 艺术模拟器

**标题**
把素材变成字符画

**正文**
最近做了个浏览器工具——ASCII 艺术模拟器。把 3D 模型、视频、图片丢进去，当场转成字符画。

核心不是堆网页元素，而是 GPU 里的 GLSL shader 逐格采样、按明暗换字符，所以拖参数不卡。

最难的是 3D：尺寸、轴心、材质各异，光靠亮度会照平。我加了法线分析通道，把深度、边缘、曲率一起喂给字符——连 0 和 1 都能看出起伏。

左键转、滚轮缩放、中键挪；默认手机 9:16；能导出 PNG / 视频 / 网页组件。

高级感，是把一种语言说到极致。
