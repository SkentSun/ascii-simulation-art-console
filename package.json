{
  "name": "ascii-media-lab",
  "version": "1.0.0",
  "description": "Realtime ASCII renderer for 3D models, images, SVG and video.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:single": "vite build --outDir outputs/ascii-media-lab-single",
    "preview": "vite preview --host 127.0.0.1"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "three": "^0.179.1"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
